# Minimal CMakeLists.txt for examples/rt1060

# Add your example source files here, e.g. dual_hid
add_subdirectory(dual_hid)

# If you have more example folders, add them similarly:
# add_subdirectory(another_example)